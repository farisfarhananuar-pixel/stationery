{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "composer install --no-dev --optimize-autoloader --no-interaction && php artisan migrate --force && php artisan db:seed --force 2>/dev/null; php artisan storage:link 2>/dev/null; php -S 0.0.0.0:$PORT -t public",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
